version: "3.8"

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-admin123}
      - DB_PATH=/app/data/jersey-archive.db
    volumes:
      - ./data:/app/data
      - ./public/images:/app/public/images
